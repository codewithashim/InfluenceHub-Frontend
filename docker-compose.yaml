version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    container_name: influence-hub-app
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development